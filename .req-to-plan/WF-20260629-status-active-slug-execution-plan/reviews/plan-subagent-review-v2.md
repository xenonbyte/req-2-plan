Verdict: Approved

## Summary

The v2 revision resolves both prior Important findings cleanly and introduces no regression. PLAN-TASK-001 now declares `tests/test_stage_templates.py` in its `Files`, declares the co-ownership with PLAN-TASK-002 through `Out-of-Task` / `Future Task Ownership`, and draws a clean T1-vs-T2 boundary (T1 = rendering/presence assertions; T2 = AC-4/AC-5 gate round-trip). The PLN-3 skeleton no longer ships the broken literal that dropped the placeholder token; it now explicitly KEEPS the author-fill `<!-- fill in: … -->` comment and enriches only its example, and Step 3 pins `test_plan_template_verification_is_a_placeholder_until_filled` to stay green. Trace/consumption/scope invariants all still hold: 8 SPEC consumed once each, 12 SCOPE-IN carried (T1 007–011, T2 012, T3 001–006), contiguous numbering 1–3, seven required fields with `python` fences per task, no SCOPE-OUT leak into gated content, dogfooded Risk Handling rows all closure-tagged. Approving.

## Resolution of prior findings

Finding 1 (Important) — RESOLVED. PLAN-TASK-001 `Files:` now lists `tests/test_stage_templates.py` (07-plan.md:18-20), removing the contradiction with its own Step 1 (`Write red rendering/presence assertions in tests/test_stage_templates.py`, 07-plan.md:68) on a `TDD Applicable: yes` task. Co-ownership is now declared on both sides: T1 `Future Task Ownership` says "PLAN-TASK-002 adds the AC-4/AC-5 gate round-trip cases to the same tests/test_stage_templates.py" (07-plan.md:76) and its `Out-of-Task` adds "does not author the AC-4/AC-5 gate round-trip cases" (07-plan.md:75); T2 `Future Task Ownership` says "PLAN-TASK-001 owns the seed implementation under test here" (07-plan.md:122). The T1-vs-T2 boundary is clean and explicit: T1 Step 1 ends with "(The AC-4/AC-5 gate round-trip cases are PLAN-TASK-002.)" (07-plan.md:68), while T2's skeleton imports `check_quality_gate` and defines `test_seeded_plan_passes_gate_when_filled_minimally` (AC-4) and `test_plan_passes_gate_when_optional_sections_omitted` (AC-5) (07-plan.md:101-111). T1 makes presence/rendering assertions only; T2 owns the gate round-trip — no overlap.

Finding 2 (Important) — RESOLVED. The v1 broken literal (`<!-- targeted command; … -->`, which dropped the `fill in` token) is gone. The v2 T1 skeleton replaces it with an explicit instruction: "the existing author-fill Verification placeholder comment is KEPT (so an untouched template still trips `_check_plan_task_verification_placeholders` and fails the gate, preserving `test_plan_template_verification_is_a_placeholder_until_filled`); only its example is enriched to the three-part shape" (07-plan.md:53-55). Step 2 repeats "PLN-3 keeps the Verification line's author-fill placeholder (only its example is enriched) so an untouched template still fails the gate" (07-plan.md:69), and Step 3 requires confirming "`test_plan_template_verification_is_a_placeholder_until_filled` stays green" (07-plan.md:70). This is correct against the code: `_FILL_IN_PLACEHOLDER_RE = re.compile(r"<!--\s*fill in(?:\s*:.*?)?\s*-->", re.IGNORECASE)` (gates.py:99) matches `<!-- fill in: …enriched example… -->`, so keeping the `fill in:` prefix and `-->` keeps the rendered template tripping the placeholder check that test 51-57 asserts is truthy. Relying on the existing test (rather than adding a new mirror assertion as v1 suggested) is sufficient — that test already pins exactly this constraint. The optional-vs-required distinction the v1 review wanted is now also explicit: the optional sections are placeholder-FREE while the required `Verification` example REMAINS a placeholder.

## Regression check

- SPEC consumption — PASS. All 8 SPEC consumed exactly once: SPEC-SEED-001→T1 (:15), SPEC-SEEDTEST-001→T2 (:80), SPEC-EXE-001..005 + SPEC-GUARD-001→T3 (:126). PLAN Trace table (:200-202) agrees.
- SCOPE-IN coverage — PASS. T1 carries 007-011 (:77), T2 carries 012 (:123), T3 carries 001-006 (:174). All 12 carried, each once; matches the expected distribution.
- Numbering & required fields — PASS. PLAN-TASK-001/002/003 contiguous from 1. Each carries all seven `PLAN_TASK_FIELDS` non-empty with a complete ```python``` Skeleton fence (T1 :22-66, T2 :86-112, T3 :134-162); all three TDD-yes, Change Type=modify.
- SCOPE-OUT leak — PASS. SCOPE-OUT-* appears only in `## Non-goals` (:288-293), which lies inside the `## Upstream Summary (read-only)` … `<!-- /r2p-read-only -->` span (:204-314). `strip_readonly_sections` (markdown.py:62; anchored by the `(?:Upstream Summary|Project Context)\s+\(read-only\)` start pattern and the `<!-- /r2p-read-only -->` end marker) removes that span before the gate scans (gates.py:959), so no SCOPE-OUT reaches `scope_out_violations`. None appears in any `### PLAN-TASK` body.
- Dogfooded Risk Handling — PASS. All six RISK-* (DRIFT/SEED/RESUME/TRUST/SCOPE/TREE) rows carry a same-line `[ADDRESSED]` (:189-194); RISK IDs live in cells only, never in a heading; mappings are genuine. `## Execution Readiness` (:177-184) accurate. Both sections sit outside the `### PLAN-TASK` bodies, so the last task body bounds at `## Execution Readiness`.
- T1+T2 co-editing tests/test_stage_templates.py — PASS / gate-acceptable. The file already exists; both tasks are `Change Type: modify` and additive (T1 adds presence/rendering assertions, T2 adds two gate round-trip functions). Neither creates the file, so there is no create/modify ordering conflict; sequential additive edits to one existing file are not blocked by any gate, and both tasks now list the file in `Files`, so the EXE-3 files-vs-`Files` reviewer discipline is satisfied for each.
- M3 (granularity-note wiring) — ADDRESSED. The v2 skeleton now shows an explicit `_HEADING_BODY[(Stage.PLAN, "## Tasks")]` edit block (07-plan.md:48-55) stating PLN-5 prepends `_PLAN_GRANULARITY_NOTE` to the `## Tasks` body, and Step 2 says "prepend the granularity note to the `## Tasks` body" (:69). The v1 mismatch (constant defined but never emitted by `template_for`) is corrected; the note is now wired through `_HEADING_BODY`, which `template_for` already renders via `_body_for`.
- M2 (no forbidden-token guard for the Context-Read prohibition) — reasoning SOUND. The EXE-5 prohibition prose itself names the phrase ("no Context-Read checklist", 07-plan.md:165; SPEC-EXE-005 "does NOT contain a self-attested 'Context Read' checklist", :236), so an `assert "Context Read" not in text` guard would self-collide with the very prose that establishes the prohibition — a false positive. Using the positive `Verification Evidence` / `Changed Files` presence tokens (:153-154) as the guard, and leaving the absence to the prose + reviewer, is the correct call. This matches the same trust level the repo already accepts for marker-presence audits.
- Residual non-blocking note (carried from v1, unchanged): the EXE-4 guard pins only `reconstruct the in-progress task's BASE` (:148) while the Test Matrix row (:278) also claims the test asserts no-`HEAD~1`/stop-and-ask. Step 4 defers exact literals to "finalize against the authored prose," and the existing SDD set already guards `HEAD~1` presence, so this is non-blocking; the implementer may add a second EXE-4 token to match the matrix.

## Ambiguity

No blocking ambiguity. The only deferred point remains the exact guard-token wording for Batch 1, explicitly deferred to "finalize against the authored prose" in Step 4 of PLAN-TASK-003 (07-plan.md:167) and SPEC-GUARD-001 (:244) — acceptable, since the chosen literals were verified absent from both surfaces in v1 and will go red-first. Both Important findings are genuinely resolved and nothing regressed.
