Verdict: Changes Requested

## Summary

The PLAN is trace-perfect, scope-clean, and gate-correct: all 8 SPEC IDs are consumed exactly once, all twelve SCOPE-IN items are carried with no orphan or double-carry, numbering is contiguous, every required field is non-empty, the dogfooded `## Execution Readiness` / `## Risk Handling` sections are gate-clean and accurate, and all six EXE-1..5 guard tokens are genuinely absent from both r2p-execute surfaces (verified by grep), so they will go red-first as intended. The EXE prose direction is fully specified by the SPEC and lands on real, verified anchors.

Two executability defects block a clean, brief-faithful execution and drive the verdict:

- F1 (Important): PLAN-TASK-001's `Files:` omits `tests/test_stage_templates.py`, yet its own Step 1 mandates writing red unit assertions in that file (TDD Applicable: yes). An implementer honoring `Files` cannot write T1's tests; one writing them violates the brief's `Files` boundary — the exact EXE-3 files-vs-`Files` discipline this run hardens. It also collides with T2, which alone declares that file.
- F2 (Important): T1's skeleton renders the PLN-3 "enriched Verification" example as `<!-- targeted command; full relevant suite command; expected evidence -->` — dropping the `fill in` token. Implemented literally, the seed no longer trips `_check_plan_task_verification_placeholders`, breaking the existing `test_plan_template_verification_is_a_placeholder_until_filled`. The enriched three-part example must retain a placeholder token.

Both are small fixes, but as written T1 cannot be executed correctly from its brief alone. Recommend Changes Requested.

## Findings

1. **[Important] PLAN-TASK-001 `Files:` is incomplete — omits `tests/test_stage_templates.py` that its own Steps write to.**
   Evidence: `07-plan.md:18-19` lists `Files:` as only `tools/workflow_cli/stage_templates.py`, but `07-plan.md:64` (Step 1) says "Write red unit assertions in tests/test_stage_templates.py …", and `07-plan.md:17` marks the task `TDD Applicable: yes`. The PLAN's own convention lists the test file for TDD tasks: T2 lists `tests/test_stage_templates.py` (`07-plan.md:80`) and T3 lists `tests/test_docs_consistency.py` (`07-plan.md:128`). T1 breaks that convention.
   Impact: This run adds the EXE-3 reviewer check "compare changed files against the brief's `Files`; a file outside that list requires recorded justification or `CHANGES_REQUESTED`" (`06-spec.md:24-27`). T1 itself would change a file not in its `Files`. A fresh implementer must either (a) skip T1's unit tests (violating Step 1 / TDD) or (b) edit `test_stage_templates.py` and trip the very files-vs-`Files` discipline being shipped. It also creates undeclared co-ownership of `test_stage_templates.py` with T2.
   Recommendation: add `tests/test_stage_templates.py` to T1's `Files`, and note in T1/T2 `Future Task Ownership` that the file is co-owned (T1: template-content unit assertions; T2: gate round-trip tests). Alternatively fold T1's unit assertions into T2, but the cleaner fix preserves the SPEC's seed/tests split.

2. **[Important] T1 skeleton's PLN-3 "enriched Verification" example drops the `fill in` placeholder and would regress an existing test.**
   Evidence: `07-plan.md:51` shows `Verification: <!-- targeted command; full relevant suite command; expected evidence -->` (no "fill in"). The current seed is `Verification: <!-- fill in: objective pass/fail check … -->` (`stage_templates.py:50-51`), and `tests/test_stage_templates.py:51-57` asserts `_check_plan_task_verification_placeholders(template_for(Stage.PLAN, STANDARD))` is truthy — i.e. the untouched seed must still trip the gate. The only placeholder pattern an HTML comment can match is `_FILL_IN_PLACEHOLDER_RE = <!--\s*fill in…-->` (`gates.py:99`); `<!-- targeted command; … -->` matches none of `_PLACEHOLDER_PATTERNS` (`gates.py:101-114`), so the check returns `[]` and the test fails.
   Impact: literal implementation turns the full suite red; a naive fix could weaken/delete the test (removing the "untouched template can't pass" protection) instead of keeping the placeholder. SPEC intent supports keeping it: PLN-3 says "Verification stays the author-supplied required field; only its example is enriched" (`06-spec.md:56`).
   Recommendation: keep a placeholder token in the enriched example, e.g. `Verification: <!-- fill in: targeted command; full relevant suite command; expected evidence -->`, and add a T1 red assertion mirroring `test_plan_template_verification_is_a_placeholder_until_filled` so the constraint is pinned. Note the distinction T1 must observe: the optional sections (`## Execution Readiness` / `## Risk Handling`) must be placeholder-FREE (so AC-4 passes with them present), while the required `Verification` example must REMAIN a placeholder (so the untouched template fails). The PLAN does not make this distinction explicit.

3. **[Minor] EXE-4 guard token pins only the derive-on-resume literal, not the no-`HEAD~1`/stop-and-ask, despite the Test Matrix wording.**
   Evidence: the chosen EXE-4 token is `reconstruct the in-progress task's BASE` (`07-plan.md:148`, `07-plan.md:198`→trace), but the Test Matrix row claims the EXE-4 test asserts "lowest-numbered unchecked task + preceding-completion `head7` reconstruction + no-`HEAD~1`/stop-and-ask present on both surfaces" (`07-plan.md:274`). `no-HEAD~1`/stop-and-ask are AC-3 content (`03-requirement-brief.md:73`) but not pinned by the listed token. The existing SDD set already guards `HEAD~1` presence (`test_docs_consistency.py:113`), so this is non-blocking; the implementer may add a second EXE-4 token to match the matrix.

4. **[Minor] EXE-5 has no forbidden-token guard for the "no Context-Read checklist" prohibition.**
   Evidence: SPEC-EXE-005 requires the report "does NOT contain a self-attested Context Read checklist" (`06-spec.md:40`), and the EXE-5 plan step repeats it (`07-plan.md:161`), but the guard only asserts presence of `Verification Evidence` and `Changed Files` (`07-plan.md:149`). The absence of a Context-Read checklist is unguarded prose. Acceptable (a forbidden token would over-pin phrasing), but worth noting the audit relies on the reviewer, not the test.

5. **[Minor] T1 `_PLAN_GRANULARITY_NOTE` wiring is shown as a standalone constant but never emitted by the skeleton's `template_for`.**
   Evidence: `07-plan.md:23-26` defines `_PLAN_GRANULARITY_NOTE`, but the skeleton's `template_for` (`07-plan.md:53-61`) only appends `_OPTIONAL_SECTIONS` and `_TRACE_SKELETON`. Step 2 (`07-plan.md:65`) clarifies the note is added to `_HEADING_BODY[(Stage.PLAN, "## Tasks")]`, and the PLN-2/PLN-3 edits are described only as a comment (`07-plan.md:47-51`), not code. A careful implementer following the Steps gets it right, but the skeleton's `template_for` could mislead. Recommend showing the `_HEADING_BODY` edit (note prepended under `## Tasks`) in the skeleton.

## Trace & consumption

1. SPEC consumption — PASS. All 8 SPEC IDs in `06-spec.md` are consumed exactly once: SPEC-SEED-001→T1 (`07-plan.md:15`), SPEC-SEEDTEST-001→T2 (`:76`), SPEC-EXE-001..005 + SPEC-GUARD-001→T3 (`:122`). No SPEC is double-consumed or orphaned; the PLAN Trace table (`:196-198`) and `plan_consumed_spec_ids` semantics agree.
2. SCOPE-IN coverage — PASS. SCOPE-IN-001..006→T3 (`:170`), 007..011→T1 (`:73`), 012→T2 (`:119`). All twelve carried, each exactly once; none orphaned, none double-carried.
3. Numbering & required fields — PASS. Tasks 001/002/003 contiguous from 1. Each carries all seven `PLAN_TASK_FIELDS` non-empty (Spec References, Change Type=modify, TDD Applicable=yes, Files, Skeleton, Steps, Verification). All three are TDD-yes and each Skeleton has a complete ```python fence (`:21-62`, `:82-108`, `:130-158`), satisfying `_plan_tasks_missing_code` / `_has_complete_code_fence`.
4. Scope overflow — PASS. No SCOPE-OUT-* id appears in any `### PLAN-TASK` body. SCOPE-OUT references exist only in `## Non-goals` (`:284-289`), which sits inside the read-only `## Upstream Summary` block (stripped by `strip_readonly_sections` before gating) and is a document-level `##` heading outside any consumed SPEC block, so `scope_out_violations` cannot see them.

## Executability

5. Files accuracy/completeness — MIXED. T1 `stage_templates.py` (exists, the single seed source), T2 `tests/test_stage_templates.py` (exists), T3 both r2p-execute surfaces + `tests/test_docs_consistency.py` (all exist and are the verified touchpoints). DEFECT: T1's `Files` omits `tests/test_stage_templates.py` that its Step 1 writes to (Finding 1).
6. Skeleton fidelity — MOSTLY FAITHFUL.
   - T1 `template_for` edit matches the real function (`stage_templates.py:60-67`): it appends `_OPTIONAL_SECTIONS.get(stage, "")` before `_TRACE_SKELETON` and changes the join to `"\n".join(p for p in parts if p)`. This is correct and preserves byte-identical output for non-PLAN stages — the empty optional string is filtered, and all existing parts are non-empty, so no extra blank line is introduced. `required_headings`/`STAGE_SCHEMA`/`PLAN_TASK_FIELDS` are left untouched.
   - T1 `_OPTIONAL_SECTIONS` content is gate-clean: the `| RISK-EXAMPLE-001 | PLAN-TASK-001 | [ADDRESSED] |` row carries a same-line `[ADDRESSED]` (clears Check 3 `_find_ids_without_closure`), and both IDs are well-formed per `_VALID_TRACE_ID_RE` (clears the R2.3b malformed-trace-ID scan). The comments/checklist carry no placeholder token in `_PLACEHOLDER_PATTERNS`. `RISK-EXAMPLE-001`/`PLAN-TASK-001` live only in table cells, never a heading, so `build_trace`/`risk_ids_not_closed` do not treat them as defined. The PLN-2 `PLAN-TASK-NNN` form has no digit, so `_TRACE_ID_CANDIDATE_RE` does not flag it — correctly gate-safe.
   - DEFECT: the PLN-3 enriched `Verification` example drops the `fill in` token and would break an existing test (Finding 2).
   - T2 fixture is realistic. `_seed` mirrors the `_check_plan`/`_plan` pattern in `tests/test_gates.py:926-937` (writes `02-project-context.json` with `repo_root=tmp`, a SPEC defining `SPEC-SEED-001`, a brief with `SCOPE-IN-001`, and the PLAN). It satisfies `_check_plan_context_pack` (repo_root is an existing dir), `_check_plan_file_refs` (achievable with a Files path that exists under tmp, e.g. a seeded artifact), `check_trace_closure` (SPEC-SEED-001 consumed, SCOPE-IN-001 carried, no RISK defined since 04 is absent), and `_check_spec_refs_valid`. The seeded `## Risk Handling` row neither creates a trace-closure RISK (cell, not heading) nor a Check-3 failure (closure-tagged). AC-5's omission case reduces to the standard passing-PLAN shape.
7. Token-absence grep (the six candidate literals) — ALL ABSENT from BOTH surfaces (exact, case-sensitive, the test uses `tok in text`):
   - `the final reviewer is the one role that reads \`07-plan.md\`` — claude: 0, codex: 0
   - `a non-clean code tree at a task boundary` — claude: 0, codex: 0
   - `against the brief's \`Files\`` — claude: 0, codex: 0
   - `reconstruct the in-progress task's BASE` — claude: 0, codex: 0
   - `Verification Evidence` — claude: 0, codex: 0
   - `Changed Files` — claude: 0, codex: 0
   All six are distinctive and will go red-first. Note (Minor): the lowercase substring "changed files" occurs inside "intentionally-changed files" (claude:178 / codex:179), but the capitalized token `Changed Files` is not a substring of it, and the guard is case-sensitive, so distinctiveness holds. EXE-3's §1 landing point is confirmed: both surfaces (claude:110 / codex:111) currently list "`Skeleton`, `Steps`, `Spec References`, and `Verification` criteria" and omit `Files`.
8. EXE prose correctness — SOUND.
   - EXE-1 preserves the per-task ACS `07-plan.md` exclusion (`06-spec.md:16`; `07-plan.md:161` "without copying the per-task `07-plan.md` exclusion") and adds the full read only to the final-review section. The `test_execute_surfaces_hand_authoritative_context_set` ACS-block check bounds the ACS block at the next `## ` heading and inspects only text before the in-block `07-plan.md` exclusion line; the `## Final Whole-Branch Review` section (claude:190) is outside that block, so the new full read does not trip it — provided the implementer adds it to the final-review section, not the ACS block, exactly as the SPEC says.
   - EXE-4 forbids `HEAD~1` and fresh-from-HEAD with stop-and-ask (`07-plan.md:161`; `06-spec.md:35`), reconstructing Task 1 from `Execution BASE:` and Task N from the preceding completion line's `head7`; lands in the existing `## Durable Progress` section (claude:222-225), which already carries "Do not recalculate it from `HEAD`".
   - EXE-5 forbids a Context-Read checklist (`07-plan.md:161`) and adds the two evidence sections.
   - Scope boundaries hold: Batch 1 touches no gate/state/CLI (T3 Out-of-Task `:168`), Batch 2 touches no `STAGE_SCHEMA`/`PLAN_TASK_FIELDS` (T1 Out-of-Task `:71`, and the skeleton leaves them untouched).

## Granularity & dogfooding

9. The 3-task split matches the SPEC's own PLAN Handoff (`06-spec.md:101-104`), which defined seed (T1) and tests (T2) as separate tasks — the PLAN keeps them separate, which is fine and improves reviewability. Merging Batch 2's five PLN seeds into one task (T1) is reasonable: they are one cohesive edit to a single file. T3 is the largest (5 EXE behaviors × 2 surfaces + guard test), but it is the indivisible "two-surface lockstep, red-first guard" unit the SPEC itself scoped as PLAN-TASK-3, completable by one implementer and reviewable by one reviewer. Each task is independently mergeable per the PLN-5 guidance the PLAN seeds. (The only granularity caveat is the Finding-1 file-ownership gap, not the task count.)
10. Dogfooded sections are gate-clean and accurate. `## Risk Handling` (`:183-189`): all six RISK-* from `04-risk-discovery.md` appear, IDs in cells only, each row a same-line `[ADDRESSED]`; mappings are genuine (RISK-DRIFT-001→T3 lockstep guard; RISK-SEED-001→T1+T2; RISK-RESUME-001→T3 EXE-4; RISK-TRUST-001→T1+T3 inert markers; RISK-SCOPE-001→all three Out-of-Task boundaries; RISK-TREE-001→T3 EXE-2 pathspec). `## Execution Readiness` (`:172-180`) is accurate (design Decision Requests is `none`; upstream approved with full closure). Both sit outside `### PLAN-TASK` bodies, so `_iter_plan_task_bodies` bounds the last task at `## Execution Readiness` as the SPEC requires.

## Ambiguity

The only legitimately deferred point is the exact guard-token wording, which the PLAN explicitly defers to "finalize against the authored prose" (`07-plan.md:157,163`; Step 4) — acceptable. No other blocking ambiguity exists; the EXE prose is fully specified by `06-spec.md`. Findings 1 and 2 are defects, not deferrals: they are concrete contradictions in T1's brief that would prevent a fresh implementer from executing the task correctly and keeping the suite green. Resolve them (and optionally the Minor items 3-5) and the PLAN is ready for execution.
