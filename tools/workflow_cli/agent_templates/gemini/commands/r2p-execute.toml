description = "Execute a closed run's PLAN in place on the current branch via the subagent-driven SDD loop, then archive. Strict is the only current execution profile. Task roles use r2p-context-view themselves for semantic_view/semantic_payload_bytes, default to targeted/directly affected verification, and final review always runs the full suite. Every role needs a new zero-history subagent session; Gemini only forwards the wrapper, so fail closed if the host cannot provide that isolation and must not claim unavailable subagent capability. Metrics are controller-owned and non-authoritative."
prompt = "!{\"{{R2P_BIN_DIR}}/r2p-execute\" {{args}}}"
